export function arraysMatch (array1: Array<number>, array2: Array<number>): boolean;

export function isZero (value: number | string): boolean;

export function parseValue (value: number | string): number;
